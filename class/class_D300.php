<?
require_once "class_top.php";
require_once "class_D.php";
require_once "class_C.php";

////////////////////////////////
// class name: class_D300
//

class class_D300 extends class_top {

    ///////////////////////////////////////////
    // shop들의 list출력

    function D300()
    {
	global $connect;
	global $template;

	$line_per_page = _line_per_page;

        $master_code = substr( $template, 0,1);
        include "template/" . $master_code ."/" . $template . ".htm";
    }

    ///////////////////////////////////////////
    // 발주 작업상태 가져오기
    function get_working_sts()
    {

    }


    function D301()
    {
	global $connect;
	global $template;

	$line_per_page = _line_per_page;

        $master_code = substr( $template, 0,1);
        include "template/" . $master_code ."/" . $template . ".htm";
    }
    function D302()
    {
	global $connect;
	global $template;

	$line_per_page = _line_per_page;

        $master_code = substr( $template, 0,1);
        include "template/" . $master_code ."/" . $template . ".htm";
    }
    function D303()
    {
	global $connect;
	global $template;

	$line_per_page = _line_per_page;

        $master_code = substr( $template, 0,1);
        include "template/" . $master_code ."/" . $template . ".htm";
    }
    function D304()
    {
	global $connect;
	global $template;

	$line_per_page = _line_per_page;

        $master_code = substr( $template, 0,1);
        include "template/" . $master_code ."/" . $template . ".htm";
    }
    function D305()
    {
	global $connect;
	global $template;

	$line_per_page = _line_per_page;

        $master_code = substr( $template, 0,1);
        include "template/" . $master_code ."/" . $template . ".htm";
    }
    function D306()
    {
	global $connect;
	global $template;

	$line_per_page = _line_per_page;

        $master_code = substr( $template, 0,1);
        include "template/" . $master_code ."/" . $template . ".htm";
    }
    function D307()
    {
	global $connect;
	global $template;

	$line_per_page = _line_per_page;

        $master_code = substr( $template, 0,1);
        include "template/" . $master_code ."/" . $template . ".htm";
    }
    function D308()
    {
	global $connect;
	global $template;

	$line_per_page = _line_per_page;

        $master_code = substr( $template, 0,1);
        include "template/" . $master_code ."/" . $template . ".htm";
    }
    function D309()
    {
	global $connect;
	global $template;

	$line_per_page = _line_per_page;

        $master_code = substr( $template, 0,1);
        include "template/" . $master_code ."/" . $template . ".htm";
    }
}

?>
